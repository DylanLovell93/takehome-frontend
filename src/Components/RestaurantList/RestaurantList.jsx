import "./RestaurantList.css";
import React from "react";
import { Typography } from "@mui/material";

const RestaurantList = ({ mobile }) => {
  return <div className="RestaurantList"></div>;
};

export default RestaurantList;
